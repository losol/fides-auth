{
  "name": "fides-auth-monorepo",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:tag": "changeset git-tag"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "turbo": "^2.10.9"
  }
}
